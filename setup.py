from setuptools import setup, find_packages

setup(
    name="genai_ops",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        # keep empty here; use requirements.txt for CI/install
    ],
)

